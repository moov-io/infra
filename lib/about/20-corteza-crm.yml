---
apiVersion: v1
kind: Service
metadata:
  name: corteza-crm
  namespace: about
spec:
  type: ClusterIP
  selector:
    app: corteza-crm
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
---
