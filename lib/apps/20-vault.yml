---
apiVersion: vault.security.coreos.com/v1alpha1
kind: VaultService
metadata:
  name: vault
  namespace: apps
spec:
  nodes: 0
  version: 0.9.1-1 # TODO(adam): wow, we really need to upgrade this 1.3.0
---

# need to expose Service ports on the deployment
