---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: ofac-webhook-example
  namespace: apps
  labels:
    app: ofac-webhook-example
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ofac-webhook-example
  template:
    metadata:
      labels:
        app: ofac-webhook-example
    spec:
      containers:
      - image: moov/ofac-webhook-example:v0.9.0-rc2
        imagePullPolicy: Always
        name: ofac-webhook-example
        args:
          - -http.addr=:8080
        ports:
          - containerPort: 8080
            name: http
            protocol: TCP
        resources:
          limits:
            cpu: 100m
            memory: 50Mi
          requests:
            cpu: 25m
            memory: 10Mi
        readinessProbe:
          httpGet:
            path: /ping
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /ping
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
      restartPolicy: Always
---
